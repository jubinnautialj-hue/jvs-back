

### 本地测试groovy 函数是否正常执行

或在界面添加函数进行测试


示例代码
```

import cn.hutool.Hutool;
import cn.hutool.core.date.DateUtil;
import cn.hutool.core.util.ArrayUtil;
import cn.hutool.core.util.ObjectUtil;
import com.alibaba.fastjson.JSON;
import com.alibaba.fastjson.JSONArray;
import groovy.lang.Binding;
import groovy.lang.GroovyShell;
import groovy.lang.Script;

import java.util.ArrayList;
import java.util.List;

public class TestFunction {

    static class Student {
        String name;
        String sex;

        public void setName(String name) {
            this.name = name;
        }

        public void setSex(String sex) {
            this.sex = sex;
        }
    }

    public static void main(String[] args) {

        List<String> pa = new ArrayList<>();
        pa.add("111");
        pa.add("222");
        pa.add("333");
        List<String> ba = new ArrayList<>();
        ba.add("111");
        ba.add("fff");
        ba.add("ddd");
//        LinkedHashMap<String, Object> pa = new LinkedHashMap<>();
//        pa.put("aaa", "1111");
//        pa.put("bbb", "2222");

        Student m = new Student();
        m.setName("tom");
        m.setSex("13");
        Object[] obj = new Object[]{m};

        Student m1 = new Student();
        m1.setName("tom");
        m1.setSex("13");
        Object[] obj1 = new Object[]{m1};
//        pa.put("ccc",s);

        String s1 = "[{\"danWei\":\"台\",\"zongJia\":14997,\"shuLiang\":3,\"danJia\":4999,\"id\":\"1567091668160671744\"}]";
        String s2 = "[{\"danWei\":\"台\",\"zongJia\":14997,\"shuLiang\":3,\"danJia\":4999,\"id\":\"1567091668160671744\"}]";
        String s3 = "[{\"danWei\":\"台\"}]";
        String d4 = "[{\"danWei\":\"台\",\"zongJia\":14997},{\"danWei\":\"213\",\"zongJia\":333},{\"danWei\":\"台\",\"zongJia\":14997}]";
        String d2 = "{\"danWei\":\"444\",\"zongJia\":555}";
        String[] d3 = new String[]{"出院", "术后第七日", "术后第一日", "入院日"};
        String[] d5 = new String[]{"出院", "术后第七日", "术后第一日", "入院日"};
//        LocalDate trans = LocalDate.parse(s);
//        long l = trans.atStartOfDay(ZoneOffset.ofHours(8)).toInstant().toEpochMilli();
//        long longtimestamp = System.currentTimeMillis();
//        long longtimestamp2 = System.currentTimeMillis();
//        Date newdate1 = DateUtil.parse("2022-10-03 12:22:33", "yyyy-MM-dd HH:mm:ss");
        String newdate1 = "2022-09-05T03:21:23.825Z";
//        Date newdate2 = DateUtil.parse("2022-10-02", "yyyy-MM-dd");
        String newdate2 = "2022-10-02";
        Object o1 = com.alibaba.fastjson.JSONObject.parse(d4);
        Object o2 = com.alibaba.fastjson.JSONObject.parse(d2);
        Object n = new Object();
        JSONArray objects1 = com.alibaba.fastjson.JSONObject.parseArray(s1);
        JSONArray objects2 = com.alibaba.fastjson.JSONObject.parseArray(s2);
        JSONArray objects3 = com.alibaba.fastjson.JSONObject.parseArray(s3);
        Binding binding = new Binding();
//        binding.setVariable("functionName", "sum");
        binding.setProperty("a1",objects1);
        binding.setProperty("a2", objects2);
        binding.setProperty("a3", objects3);
        GroovyShell shell = new GroovyShell(binding);
        String str = "def SIN(x){ return Math.sin(x)};SIN(30)";
        Script parse = shell.parse(str);
        Object run = parse.run();
        Object evaluate = shell.evaluate(str);
        System.out.println(evaluate);
    }
}
```

### 将最后的函数组装为 sql，直接操作数据库

```sql
INSERT INTO `jvs-design`.`jvs_function_base` ( `name`, `type`, `info`, `param_count`, `dynamic_param`, `enable_cache`, `body`, `jvs_param_type`) VALUES ('CONNECTARRAY', '集合函数', 'xxx', '[2]', 1, 1, 'def CONNECTARRAY(x,... p){    def result = []; x.each { it-> def s= []; p.each {it2-> s.add(it[it2]);}; result.add(s.join(\'-\')); }; return result.join(\'\\n\')};', 'text');
```
