## 后端开发组件过程示例

·以签名组件为例


### 1、 添加组件类型声明
在`cn.bctools.design.data.fields.enums.DataFieldType`类中添加组件信息


```
public enum DataFieldType {
    ....
    signature("手写签名", FormComponentType.advance, false, String.class, true, true),
	....
}
```

###  2、添加组件数据格式对应的实现类并同时继承`BaseItemHtml`,这里由于签名为图片。所以与文件、图片使用相同的类，此类的参数定义与前端组件设计相关。具体见[`前端开发组件过程示例`](http://doc.bctools.cn/#/knowledge/my/d47f2e9811f98800a7553137a73fe5c7?docId=534671cced9863a801373ea9a6d528e4&folderId=74f7f86504f72318fdeae91816201101 "`前端开发组件过程示例`")
```
@Data
@ApiModel(value = "图片上传")
public class ImageUploadHtml extends BaseItemHtml {
}
```

### 3、添加此组件的数据解析或转换实现类并同时指定数据格式类为`ImageUploadHtml`
```
@Slf4j
@Component
@DesignField(value = "手写签名", type = DataFieldType.signature)
public class SignatureFieldHandler implements IDataFieldHandler<ImageUploadHtml> {

    @Autowired
    OssTemplate ossTemplate;
	
    /**
     * 将数据库存储的数据转换为界面展示的数据，因为签名的图片数据存在安全问题，所有每一次获取签名图片都会生成一个新的临时链接地址不会永久生效。
     * @param override 是否覆盖子数据的原始值
     * @param fieldDto 回显字段的设计数据
     * @param data     数据值
     * @param lineData 原始行级数据，用于逻辑的字典转换，如， 省市区县， 多个字段进行联合转换时
     * @return 显示值
     */
    @Override
    public Object getEchoValue(ImageUploadHtml fieldDto, Object data, boolean override, Map<String, Object> lineData, String... paths) {
        //处理公共桶图片兼容显示问题
        if (ObjectNull.isNotNull(data)) {
            if (data instanceof List) {
                return JSONArray.parseArray(JSONObject.toJSONString(data), BaseFile.class)
                        .stream()
                        .peek(e -> {
                            if (ObjectNull.isNull(e.getName())) {
                                e.setName(e.getOriginalName());
                            }
                            if (ObjectNull.isNotNull(e.getBucketName(), e.getFileName())) {
							    //产生新的链接地址
                                e.setUrl(ossTemplate.fileLink(e.getFileName(), e.getBucketName()));
                            }
                        })
                        .collect(Collectors.toList());
            } else if (data instanceof String) {
			    //做兼容数据处理
                return Arrays.asList(new BaseFile().setUrl(data.toString()).setBucketName("jvs-form-design"));
            }
        }
        return data;
    }

}
```

